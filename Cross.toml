[target.aarch64-unknown-linux-gnu]
pre-build = [
  "dpkg --add-architecture arm64",
  "apt-get update",
  "apt-get install -y libasound2-dev:arm64 libasound2-dev pkg-config",
]
env = {
  PKG_CONFIG_ALLOW_CROSS = "1",
  PKG_CONFIG_PATH = "/usr/lib/aarch64-linux-gnu/pkgconfig",
}
