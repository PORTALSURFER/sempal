export struct SourceRow { name: string, path: string }

export struct WavRow {
    name: string,
    path: string,
    selected: bool,
    loaded: bool,
    tag_label: string,
    tag_bg: color,
    tag_fg: color,
}
