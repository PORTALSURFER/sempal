export struct SourceRow { name: string, path: string }

export struct WavRow {
    name: string,
    path: string,
    selected: bool,
    loaded: bool,
    bg: color,
    hover_bg: color,
    pressed_bg: color,
    border_color: color,
    indicator_color: color,
    overlay: color,
    tag_label: string,
    tag_bg: color,
    tag_fg: color,
}

export struct CollectionRow {
    id: string,
    name: string,
    selected: bool,
    count: int,
}

export struct CollectionSampleRow {
    path: string,
    source: string,
}
